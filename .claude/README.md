# Claude project files

Files under `.claude/rules/`, `.claude/skills/`, and `.claude/agents/` are
project source and may be tracked when intentionally part of this repository.
Machine-local settings, credentials, logs, transcripts, caches, and
`.claude/worktrees/` remain ignored by `.gitignore`.
