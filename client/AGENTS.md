# Archived Client Guide

This `client/` tree is archived legacy code, not an active MyDevEnv2 product
surface.

Before changing files here:

- Read `/home/sprooty/Working/AGENTS.md` for workspace-wide rules.
- Read `../AGENTS.md` for the active MyDevEnv2 repo guidance.
- Read [`README.md`](README.md) and [`UPLIFT.md`](UPLIFT.md) in this directory
  for historical context.

Rules for this tree:

- Do not treat this client as a supported product surface.
- Do not add new CI, release, deploy, or backlog flow for this tree unless the
  user explicitly asks to revive it.
- Active project backlog lives only in [`../uplift.md`](../uplift.md).
- Prefer changes in `server/`, `web/`, `mobile/`, or shared contracts when the
  request concerns the current product.
