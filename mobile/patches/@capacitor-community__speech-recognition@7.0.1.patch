diff --git a/android/build.gradle b/android/build.gradle
index d31e44278660dfa7d125ac5e7053fb5988610164..b46f663650a80b008fd81044353751fc705c72cc 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -30,7 +30,13 @@ android {
     buildTypes {
         release {
             minifyEnabled false
-            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
+            // proguard-android-optimize.txt, not proguard-android.txt: AGP 9.0
+            // removed the latter (it bundles -dontoptimize). This is a library
+            // release buildType with minifyEnabled false, so these proguardFiles
+            // are dead config, but AGP 9.0 evaluates getDefaultProguardFile at
+            // configuration time and hard-fails on the removed name. Patched via
+            // pnpm patchedDependencies until the plugin releases an AGP-9 build.
+            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
         }
     }
     lintOptions {
