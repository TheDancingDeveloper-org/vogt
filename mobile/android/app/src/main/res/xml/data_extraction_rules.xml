<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup/transfer rules. Exclude everything: the WebView
  localStorage holds the bearer token used to reach the dev pod, and we don't
  want it copied via cloud backup, adb backup, or device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
