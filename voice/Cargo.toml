[workspace]
resolver = "2"
members = ["server", "stt", "tts", "models"]

[workspace.package]
edition = "2021"
license = "MIT"
repository = "https://github.com/TheDancingDeveloper-org/vogt"
rust-version = "1.80"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
