[package]
name = "vogt-engine-contract"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/TheDancingDeveloper-org/vogt"
rust-version = "1.80"

[lib]
name = "vogt_engine_contract"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["serde"] }
