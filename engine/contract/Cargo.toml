[package]
name = "mydevenv2-contract"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://repo.indexarr.net/indexarr/MyDevEnv2"
rust-version = "1.80"

[lib]
name = "mydevenv2_contract"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["serde"] }
