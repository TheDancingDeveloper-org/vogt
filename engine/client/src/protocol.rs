//! Shared wire types for the MyDevEnv2 server API.
//!
//! The canonical DTO definitions now live in the `mydevenv2-contract` crate so
//! the server and native client consume the same Rust types.

pub use mydevenv2_contract::*;
