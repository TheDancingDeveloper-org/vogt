[workspace]
resolver = "2"
members = ["server", "contract"]

[workspace.package]
edition = "2021"
license = "AGPL-3.0-only"
repository = "https://github.com/TheDancingDeveloper-org/vogt"
rust-version = "1.80"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
